export default {
  hoverDuration: 2000,
  namespace: 'bp',
  userPreferenceObjectVersion: 'v1',
};

// no user pref attribute should be in this object
export const defaultPlayerConfig = {
  // playlistItem
  autoplay: false,
  volume: 0.5,
  aspect: 'player',
  aspectRatio: '16:9',
  // general config
  lang: 'en',
};
